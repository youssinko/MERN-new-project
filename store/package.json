{
  "name": "store",
  "version": "1.0.0",
  "description": "",
  "proxy": "",
  "main": "index.js",
  "scripts": {
    "build":"cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start":"node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod-build": "cd backend && npm install  && cd ../frontend && npm install && npm run build", 
    "prod-start": "node backend/server.js"
  },
  "author": "",
  "license": "ISC"
}
